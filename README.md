# SNTZR

## Installation

Run:

```bash
pipx install -e --force .
```

## Usage

Run:

```bash
sntzr --help
```

## Install from local directory

Clone the repo and execute the command:

```bash
pipx install -e --force .
```
